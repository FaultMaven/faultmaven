# Investigation Invariant Enforcement Matrix

The invariant registry for FaultMaven's investigation lifecycle — every load-bearing lifecycle rule with its enforcement-tier classification, pinning tests, and drift notes. Extracted from [investigation-lifecycle-logic.md](./investigation-lifecycle-logic.md) so the matrix can be maintained and audited as a standalone reference; the invariants index the rules defined throughout §1–§4 of that document.

**Related Documents**:

- [Investigation Lifecycle Logic](./investigation-lifecycle-logic.md) — the state transitions, unified opportunistic flow, and turn tracking the invariants below pin
- [Investigation Data Models](./investigation-data-models.md) — the schemas (Pydantic validators, DB CHECK constraints) that realize the Schema-tier enforcements

> **Section references.** Bare `§N` references in the *Source* column (e.g. "§1.2", "§1.4", "§2", "§2.5") point to sections of [investigation-lifecycle-logic.md](./investigation-lifecycle-logic.md). Cross-document references are written out explicitly.

---

## Invariant Enforcement Matrix

Every load-bearing lifecycle rule has at least one enforcement surface: code, schema, API, or prompt. The categories below carry *decreasing* strength — what's *structural* cannot be violated by construction; what's *prompt-only* depends on LLM compliance, which is stochastic and prone to drift across model versions and prompt edits.

**What this matrix is.** A compact index of lifecycle invariants with their enforcement-tier classification and pinning tests. It serves three functions:

1. **Risk catalog** — the Enforcement column tells you at a glance which invariants are weakest (Prompt-only > Code-guarded > Schema > Structural). Weaker-tier invariants warrant more test investment and more careful PR review.
2. **Test registry** — every row commits to at least one mechanical pin. Writing a row forces the question "what test pins this?"; gaps are visible.
3. **Single-diff change surface** — when a refactor moves an invariant to a weaker tier, the diff is visible in one place. Code review can catch the regression risk explicitly.

**What this matrix is not.** It is not a drift detector. Tables don't fire — they index. Drift detection happens at test-execution time (mechanical pins), in PR review (when the matrix surfaces tier changes), and via runtime telemetry (separately scoped). Re-reading the matrix doesn't catch a system that has silently stopped achieving its design goal — see *dynamic drift* below.

**Dynamic drift.** A failure-mode class where the system as a whole stops achieving its design goal while every individual piece (matrix row, test, doc section, code, prompt) looks locally correct. It arises from cooperation between enforcement tiers — a code-guarded check depending on a prompt rule that has independently drifted, or two prompt rules with an implicit contract that one of them breaks. No static instrument catches this class: tests pass, the matrix stays consistent, code review is clean. Only runtime mitigations catch dynamic drift: real-LLM integration tests at PR time, outcome telemetry post-deploy (e.g., a *guard-fired-to-transition* ratio), and happy-path canary probes. At design time, the one static instrument that helps is **naming composition seams explicitly** — see the *Composition seam* line on rows where enforcement crosses tiers. Future audits can then verify both sides of the dependency rather than discovering it post-regression. A worked example of the runtime-telemetry mitigation is the **grounding composition seam** — the causal-graph grade (`grade_cause_assurance`) vs the hypothesis-layer `cause_state`, which read different root sources and can silently disagree: a per-turn DEBUG-level `grounding_assessment` trace (`milestone_engine._log_grounding_assessment`) emits both readings plus a `seam_divergence` flag, turning that dynamic drift into a greppable/alertable signal (see `insufficient-evidence-handling.md` §3.5).

**Enforcement legend:**

- **Structural** — impossible to violate by construction (e.g., a state change that requires two separate function calls separated by case persistence and an LLM turn).
- **Code-guarded** — an explicit `if` / assertion in the engine blocks the bad path.
- **Schema** — Pydantic validator or DB CHECK constraint.
- **API-level** — middleware or route-handler rejects the bad request.
- **Prompt-only** — the LLM is instructed but the engine doesn't enforce. Weakest category; should be reserved for stylistic rules or rules with downstream code/structural backstops.

| # | Invariant | Source | Enforcement | Test |
|---|---|---|---|---|
| INV-01 | INQUIRY → INVESTIGATING requires the user to confirm a `proposed_problem_statement` that was presented on a **prior** turn. The LLM cannot collapse the handshake into a single turn. | §1.2 *Two-Step Confirmation Flow* (above); `INQUIRY_TEMPLATE` ("Never set `user_confirmed_investigation=True` on the same turn you first present the problem statement") | **Code-guarded at three layers.** (1) Same-turn guard: `_apply_inquiry_updates` captures `_statement_existed_before_turn` and gates the confirmation branch on it. Same-turn confirmations log a WARNING, set `case.inquiry.handshake_deferred_at_turn = current_turn`, and are deferred. (2) Context-side recovery: on the turn immediately after a guard fire, `context_builder` switches the inquiry_state block from `NOT_YET_CONFIRMED` to `HANDSHAKE_DEFERRED` (instructing the LLM to re-present + ask). (3) Engine-owned Gate 1 affordance: `engine_owned_affordances(case, metadata)` in `milestone_engine.py` returns `("gate1", _investigation_confirmation_suggestions())` on **every** Gate-1-pending turn — not only the handshake-deferred recovery turn. This is the architectural completion that makes Gate 1 symmetric with Gate 2 and Gate 3: the engine owns the canonical clickable affordance whenever a `proposed_problem_statement` awaits confirmation, regardless of whether the LLM emits its own confirmation-shaped suggestions or LLM provider drift affects optional output fields like `intent`. *Composition seam:* the prompt-side `HANDSHAKE_DEFERRED` re-present instruction and the engine-side affordance emission are tied — removing or weakening the prompt block does not break the invariant (buttons still emit, user can still click) but degrades UX: confirmation buttons appear next to a response that doesn't ask the question. Audit `context_builder.py:inquiry_state_str` and `milestone_engine.py:engine_owned_affordances` together when either changes. *Outcome telemetry:* `faultmaven_inquiry_handshake_deferred_total` paired with `faultmaven_inquiry_handshake_recovered_total` (recovery ratio for the same-turn-guard fire path); `faultmaven_engine_owned_affordance_served_total{gate="gate1"}` counts every turn where the engine substituted the Gate-1 affordance pair — a sustained zero on `gate1` with non-zero INQUIRY volume signals the predicate or consolidator has regressed silently. See [`docs/operations/monitoring/lifecycle-metrics.md`](../../operations/monitoring/lifecycle-metrics.md). | `test_inquiry_transition::test_same_turn_confirmation_is_rejected`; `::test_confirmation_accepted_when_statement_persisted_across_turns`; `TestHandshakeDeferredRecovery::test_handshake_deferred_block_injected_on_recovery_turn`; `::test_not_yet_confirmed_block_used_when_flag_is_stale`; `::test_deterministic_confirmation_suggestions_on_recovery_turn`; `TestEngineOwnedGate1OnFirstDetect::test_first_detect_turn_emits_deterministic_confirmation_pair`; `test_engine_owned_affordances::TestGate1Predicate::*`; `test_engine_owned_affordances::TestEngineOwnedAffordances::test_gate1_pending_returns_confirmation_pair` |
| INV-02 | INQUIRY → INVESTIGATING never auto-fires on CRITICAL/HIGH urgency alone — confirmation is still required regardless of severity. | §1.2 *Two-Step Confirmation Flow* — "Even for CRITICAL + ongoing issues" | **Code-guarded** — the urgency branch in `_apply_inquiry_updates` logs only; the transition gate still requires explicit `user_confirmed_investigation=True`. | `test_inquiry_transition::test_critical_outage_stays_inquiry_until_confirmed` |
| INV-03 | Disposition transitions (INVESTIGATING → RESOLVED, INVESTIGATING → CLOSED, INQUIRY → CLOSED) NEVER auto-fire. The agent emits `ProposedTransition`; the user confirms on a subsequent turn. | §1.2 *INVESTIGATING → RESOLVED (Disposition)*; §1.4 line 488 ("Disposition actions are NEVER automatic") | **Structural** — `propose_transition` writes `pending_transition`; only `confirm_pending_transition` executes the state change. The two functions cannot be called within the same `process_turn` invocation without an intervening case save and LLM turn. *Composition seam:* the structural propose+confirm split preserves the no-auto-fire invariant regardless of prompt behavior, but the user-facing confirmation UX depends on (a) the engine setting `metadata["override_suggestions"]` at `propose_transition` call sites and (b) the LLM surfacing a "please confirm" cue in `agent_response` per `_AMBIGUITY_FIRST_RULE`. Removing the prompt rule does not break the invariant (buttons still emit deterministically; users can still confirm by typing) but does degrade UX: the buttons appear next to a response that doesn't acknowledge them. Audit `propose_transition` call sites in `milestone_engine.py` and the `_AMBIGUITY_FIRST_RULE` block in `templates.py` together when either changes. | `test_inv03_*` in `test_lifecycle_invariants.py` (5 tests pin the function-level contract: propose writes pending only, confirm-without-propose is a no-op, full handshake executes only via explicit confirm, decline clears pending) |
| INV-04 | INQUIRY → RESOLVED has no direct edge. Every RESOLVED case flows through INVESTIGATING — even KB-matched cases. | §1.3 (line 442); `ALLOWED_ACTIONS` dict + `is_valid_action()` | **Schema** (`is_valid_action()` in `models.py` runs as a Pydantic model_validator on every `CaseAction` instantiation — `CaseAction` is `frozen=True` so the audit history cannot record the forbidden transition) + **Code-guarded** (`_execute_resolved_transition` raises `ValueError` on non-INVESTIGATING input — runtime backstop). `ALLOWED_ACTIONS` in `case_action_manager.py` is UI-affordance only (drives `get_allowed_transitions`). | `test_inv04_*` in `test_lifecycle_invariants.py` |
| INV-05 | The derived stage label within INVESTIGATING (Investigating / Mitigating / Resolving) follows the action-compliance gates AUTOMATICALLY — NO User-Agent Handshake. The "Mitigating" label is driven by the single `progress.mitigation` record (accepted ∧ ¬verified), not by `mitigation_*` booleans. This is the only place where LLM-emitted compliance signals change derived state without explicit user confirmation, by design. | §1.4; §2 | **Schema-derived** — `InvestigationProgress.current_stage` is a pure computed property over the mitigation record + `solution_accepted`/`solution_verified`, with no handshake plumbing. The mitigation record's own validator enforces `verified ⇒ accepted`. | `test_inv05_*` in `test_lifecycle_invariants.py` (initial stage is Investigating; an accepted-but-unverified mitigation shows Mitigating with no pending_transition; solution_accepted shows Resolving; static guard that the property body contains no handshake tokens) |
| INV-06 | The KB-Resolution same-turn collapse (INVESTIGATING → RESOLVED in one turn for runbook-matched cases) goes through `propose_transition` + `confirm_pending_transition` — the same mechanism as the multi-turn path. The engine fires confirm in the same turn ONLY when both `transition_proposed_this_turn` and `knowledge_resolution_signalled` metadata flags are set; the user's runbook-confirmation message ("it worked") is the implicit disposition acknowledgment. Every other ProposedTransition emission still follows the standard 2-turn handshake. | §1.2 *KB-Resolution Path* (lines 380-385); §4.2 | **Structural** — uses the same `pending_transition` mechanism as the multi-turn path. The same-turn collapse is the only deviation, scoped via metadata-flag conjunction. *Composition seam:* the same-turn collapse trigger depends on the LLM emitting `knowledge_resolution` in `state_updates` when a runbook fix is acknowledged — the engine's conjunction in `_check_automatic_transitions` cannot fire without the prompt-driven signal. Removing the prompt instruction to emit `knowledge_resolution` does not break the invariant (multi-turn fallback works; cases reach RESOLVED via the standard 2-turn handshake) but defeats the KB-resolution UX optimization. Audit the `knowledge_resolution` emission rule in `INVESTIGATION_BASE` (`templates.py`) and the conjunction gate in `milestone_engine.py:_check_automatic_transitions` together when either changes. | `test_inv06_*` in `test_lifecycle_invariants.py` |
| INV-07 | Evidence rows are born only during INVESTIGATING. No Evidence creation during INQUIRY — `InquiryStateUpdate` has no `evidence_to_add` field. Uploads during INQUIRY persist as `UploadedFile` only. | §1.2.1 *Core principles* | **Schema** — `InquiryStateUpdate` Pydantic model does not declare `evidence_to_add`; engine `_apply_inquiry_updates` has no evidence-creation branch. The model uses `extra='ignore'` (Pydantic default) deliberately — LLM emissions of unknown fields are silently dropped to preserve graceful degradation rather than failing the turn; the invariant is enforced by field absence, not by rejecting strays. | `test_inv07_*` in `test_lifecycle_invariants.py` |
| INV-08 | Every Evidence row has a known source: `source_file_id` set, **or** `source_type=USER_DESCRIPTION` for chat-quote evidence. There is no escape hatch. | §1.2.1 lines 215-222 | **Schema + DB** — Pydantic validators on `Evidence` and `EvidenceToAdd`; DB CHECK constraint `evidence_source_invariant` (migration 010). | exhaustive evidence-model tests |
| INV-09 | Terminal cases (RESOLVED/CLOSED) are immutable: no new evidence, no transitions, no milestone updates. Only text Q&A, report regeneration, and runbook creation are permitted. | §1.7 *Terminal Mode* (lines 1039-1080) | **API-level** — `require_case_not_terminal()` helper function (used at the case-update endpoint; other write endpoints inline the `case.is_terminal` check). **Code-guarded** — `_process_turn_impl` short-circuits to `_process_terminal_turn` for terminal cases, bypassing the milestone-engine state-mutation pipeline. | `test_inv09_*` in `test_lifecycle_invariants.py` |
| INV-10 | `submit_turn` on a terminal case: text query → routed to terminal Q&A; files / pasted content → 409 Conflict; status-transition intent → 409 Conflict. | §1.7 *Terminal Mode* (lines 1072-1078) | **API-level** — `submit_turn` endpoint inspects payload kind. | `test_inv10_*` in `test_lifecycle_invariants.py` (3 static-source guards on `routes.submit_turn`: files/pasted_content + 409 rejection block; status_transition + 409 rejection block; no unconditional rejection of text queries) |
| INV-11 | Auto-generated `CLOSURE_SUMMARY` is gated on **investigation substance** (`evidence>0` OR `hypotheses>0` OR `completed_milestones>0`). The verdict is stable post-closure because all three signals are immutable in CLOSED state. `RESOLUTION_SUMMARY` always generates. | §1.7.3, §4.5.0 | **Code-guarded** — `should_generate_terminal_summary` in `terminal_transitions.py` | `test_milestone_engine::test_summary_guardrail_*`; `::test_skip_reason_*` |
| INV-12 | Free-typed paraphrases of regen/runbook intent ("recap", "summarize", "new runbook please") route to terminal Q&A and **never** produce a persisted Report or Runbook side effect. Only **exact-match** of the DECIDE-suggestion payload triggers a persisted side effect. | §1.7.3 *Regeneration* (free-text routes to Q&A) | **Code-guarded** — `_REPORT_REGEN_PATTERNS` and `_RUNBOOK_CREATION_PATTERNS` use exact-match (`msg_lower in patterns`); paraphrases fall through to `_process_terminal_qa`. | `test_inv12_*` in `test_lifecycle_invariants.py` (4 tests: exact payload triggers regen; free-typed recap/summarize paraphrases route to Q&A; runbook paraphrases route to Q&A; pattern tuples stay in lockstep with DECIDE-suggestion payload constants) |
| INV-13 | The closure-acknowledgment turn's suggestion set depends on whether summary generation succeeded. **Success path**: RESOLVED offers the runbook affordance only (no regen); CLOSED is silent. The freshly-rendered inline summary is right above; regen alongside would be noise. **Failure path**: regen IS offered on the ack-turn — `_select_ack_follow_ups` returns `_resolved_suggestions()` (regen + runbook) for RESOLVED and `_closed_suggestions(case)` (regen, since substance had to PASS for generation to be attempted) for CLOSED. Regen is otherwise offered on subsequent terminal Q&A turns when the substance gate would PASS. | §1.7.3 *Regeneration: Where it's offered* | **Code-guarded** — `_select_ack_follow_ups` in `milestone_engine.py` switches on the `summary_failed` flag returned by `_auto_generate_report`. All three ack-turn call sites (explicit confirm, dropdown second-click, LLM-driven transition) route through this single helper. | `test_inv13_*` in `test_lifecycle_invariants.py` (4 tests pin the helper contracts on the success path: resolved-ack offers runbook only with no regen; resolved Q&A offers both regen + runbook; closed Q&A offers regen only when substance gate passes, never runbook; ack ⊂ Q&A by construction) + `test_runbook_completion_and_summary_failure.py::TestAckTurnFollowUpsOnFailure` (4 tests pin the failure-path branch of `_select_ack_follow_ups`) |
| INV-14 | Manual case-action requests (status dropdown) flow through the same confirmation pattern as natural progression — they cannot bypass the User-Agent Handshake. | §1.5 *Core Principle* — "all case actions require explicit user confirmation" | **Structural** — the UI sends a system message that routes through `submit_turn` + the standard `pending_transition` mechanism. *Composition seam:* dropdown-driven dispositions inherit the same confirmation-pair UX seam as INV-03 — the dropdown's system message (from `CASE_ACTION_MESSAGES` in `case_action_manager.py`) is a fixed string, but the LLM's `agent_response` is expected to surface a "please confirm" cue per `_AMBIGUITY_FIRST_RULE` alongside the engine-emitted override suggestions. Audit the same surfaces as INV-03. | `test_inv14_*` in `test_lifecycle_invariants.py` (4 tests pin that dropdown-initiated INQUIRY→CLOSED / INVESTIGATING→CLOSED / INVESTIGATING→RESOLVED writes pending_transition and leaves `case.state` unchanged this turn); cross-references: `test_transition_alignment.py::test_ui_dropdown_*` (canonical confirmation-pair emission) and `test_investigation_lifecycle.py::test_explicit_ui_resolve_proposes_then_confirms` (two-turn end-to-end) |
| INV-15 | The agent is an **ADVISOR** — it never runs commands, accesses systems, or makes infrastructure changes. Stated in the agent's vocabulary constraint (banned/required phrase table). | §1.6 *Agent Role Constraints* | **Prompt-only** + light vocabulary check. The runtime scan (`_completion_phrases` at `milestone_engine.py:3088`, consumed at `:3148`) is deliberately narrow — it covers the highest-stakes drift (false completion claims like *"case closed"*, *"marking as resolved"*) where the signal/noise ratio is strongest. Action-claim phrases from `_ADVISOR_ROLE_CONSTRAINT` (*"Let me check"*, *"I will run"*) are not scanned — they have higher false-positive rates in legitimate context (quoting the user, hedge phrases). Broader advisor-role drift detection is deferred to a separate `advisor_role_compliance` telemetry signal if/when operational evidence warrants it. | `test_inv15_*` in `test_lifecycle_invariants.py` |
| INV-16 | The LLM is the **sole authority** for milestone advancement. `validate_milestone_claims` in the evidence processor is read-only — it returns validation results but never writes to `case.progress.*`. Milestone state changes flow ONLY from the LLM's structured output through the milestone engine. | §3.1 *Issue A* (keyword-discovery dual pathway removed) | **Code-guarded by construction** — `validate_milestone_claims` returns `List[MilestoneValidationResult]` and contains no `case.progress.<field> =` assignments. Static-source guard pins this so any future regression (a refactor reintroducing a write inside the evidence processor) breaks the test. | `test_inv16_*` in `test_lifecycle_invariants.py` |
| INV-18 | Runbook generation is RESOLVED-only. CLOSED cases are not eligible regardless of `closure_reason`. Both the chat-side dispatcher and the API endpoint reject non-RESOLVED cases. | §4.5.1 — *"Eligibility: RESOLVED cases only."* | **Code-guarded at two layers** — (1) Engine: `_process_terminal_turn` gates dispatch on `case.state == CaseState.RESOLVED`; non-RESOLVED falls through to terminal Q&A. (2) API: `POST /knowledge/convert-from-case` returns HTTP 400 when `case_status != "resolved"` (`conversion_routes.py:507`). | `test_inv18_*` in `test_lifecycle_invariants.py` |
| INV-25 | **Only an authority-grounded cause may auto-seed the KB (§7 harvest soundness).** A runbook is auto-harvestable from a case only when its cause is graded `CONFIRMED` — ≥1 VALIDATED root borne out by a **counterfactual confirmation** (a SUPPORTS evidence link backed by a `causal_absence_evidence` row on that root; M2 gone⇒gone). A validated-but-unconfirmed cause (`MECHANISTIC` — empirical rung evidence or a deductive derivation; deduction rests on model-mediated refutations plus an asserted-exhaustive differential, so it does not clear the bar alone), or a bare `RootCauseConclusion` with no validated causal graph (`NO_ROOT`), is held back — a confidently-wrong LLM that authored both the cause and its citation must not promote an unconfirmed cause into reusable knowledge. (Orthogonal to INV-18's RESOLVED-only eligibility: a case can be RESOLVED yet non-`CONFIRMED`.) | `two-dimensional-hypothesis-methodology.md` §9.5; `document-to-runbook-conversion.md` §1.1 | **Code-guarded at two layers, single source of truth** — `cause_assurance.grade_cause_assurance(case)` is the one classifier; both enforcement layers read it, so they can't diverge. (1) API: `POST /knowledge/convert-from-case` returns HTTP **422** when the grade is not `CONFIRMED`. (2) Chat: the runbook DECIDE suggestion is offered on RESOLVED turns, but **acting on it** routes through `_handle_runbook_creation`, which runs `evaluate_runbook_suggestion` → `assess_runbook_readiness` (the grade read via `grade_cause_assurance`); a non-`CONFIRMED` cause yields `NOT_READY` (a "not ready" message, **no draft side effect**), never a runbook. The chat gate is at action time, not emit time. *Rejected alternatives:* gating on the negative "fallback-only" predicate let `NO_ROOT` through (it is not "fallback-only"); a deductive-only top grade let a chain of model self-claims (mislabelled sibling refutes + an asserted-exhaustive differential) reach harvest. | `test_conversion_flow.py::TestConvertFromCaseSoundnessGate` (rejects unconfirmed empirical + unconfirmed deductive + no-validated-root with 422; allows counterfactually confirmed); `test_milestone_engine.py::TestReadinessAssessments` (harvest NOT_SUITABLE for unconfirmed + no-validated-root); `test_derive_node_states.py` (grade classification incl. the unlinked-absence bearing pin) |
| INV-22 | **`cause_state` is an engine-derived truth signal that is NEVER path/state-stripped.** It records *what the engine knows about the cause* — recomputed every INVESTIGATING turn from the LLM's grounded cause signal plus the active-hypothesis count, independent of stage or any (now-removed) path. There is no window in which a legitimately-known cause is suppressed; this is the linchpin that dissolved the old self-naming-error trap (a case that identified its cause at turn 5 but stayed UNRESOLVED because the path forbade RCA-side emissions). | §2, §2.5 | **Code-guarded** — `_recompute_assessment_state(case)` in `milestone_engine.py` is called at the end of `_apply_investigation_updates` on every turn and sets `cause_state` from `HypothesisManager.count_active_hypotheses` + the grounded signal. The reasoning-validation strip (INV-23) operates on emitted milestones, never on the recomputed assessment enum. The justification bar accepts a self-naming-error extract as a valid basis for IDENTIFIED. | `tests/unit/core/investigation/test_surgical_strip_and_cause_state.py` (cause_state derivation across 0/1/≥2 hypotheses + grounded signal; self-naming-error acceptance) |
| INV-23 | **The reasoning-validation strip is per-milestone surgical.** When `validate_reasoning_first` rejects an emission for missing justification, only the *offending* milestones are reset to None — co-emitted valid milestones survive. A single unjustified milestone never wipes the others. (Global failures — no `internal_reasoning`, no actionable evidence — still implicate every completed milestone; turn-reference format errors implicate none.) | §2 | **Code-guarded** — `validate_reasoning_first` returns the *set* of offending milestone names; the apply-path caller in `milestone_engine.py` strips only that set. Replaces the prior wholesale-wipe degradation branch that destroyed valid `mitigation_accepted` / `mitigation_verified` alongside one forbidden emission (the S1 collateral-wipe bug). | `tests/unit/core/investigation/test_surgical_strip_and_cause_state.py` (surgical strip preserves co-emitted valid gates) |
| INV-24 | **The mitigation record is a single forward-only insert.** There is one `progress.mitigation: MitigationRecord` per investigation; its own validator enforces `verified ⇒ accepted`. Acceptance/verification are never reset (no MITIGATION re-entry). `completed_at_turn` is stamped once when `verified` first flips True (the boundary for up-weighting pre-mitigation evidence in later RCA). A non-mitigating insert is not a dead-end — the flow stays open to user-led action (alternative fix in prose, escalate, new data, or CLOSE). | §2, §2.5 | **Schema** — `MitigationRecord._verified_requires_accepted` model_validator. **Code-guarded** — the engine materializes the record from the LLM gate signals and stamps `completed_at_turn` idempotently; `solution_ordering` on `InvestigationProgress` keeps `solution_verified ⇒ solution_accepted`. | `test_inv05_*` / mitigation-lifecycle tests in `test_lifecycle_invariants.py`; `MitigationRecord` `verified ⇒ accepted` validator tests in `tests/unit/core/investigation/test_milestone_order_rejection.py` |
| INV-26 | **The pending-transition gate never consumes substantive input, and gate overrides never hide the LLM's reply.** (a) A typed gate answer must be BARE: matcher tokens match on word boundaries ("yesterday…" is not "yes", "note…" is not "no"), and a confirm-matched message carrying a question mark or a contrastive " but " is substantive input, not consent. A message that is neither a bare confirm nor a bare decline is re-presented at most ONCE (short, question-free messages only; blank input re-presents without consuming the allowance); a substantive message (long, or containing a question) — or any second non-answer — withdraws the proposal (`cancel_pending_transition`) and is processed as a normal turn. The gate can stall a case for at most one short turn; it can never brick it, and it can never *execute* a terminal transition on substantive input. (b) On gate-override turns (resolution needs-info / suggest-close / ready-confirmation / RCA-infeasible closure), the engine's canned message is appended below the LLM's `agent_response` (`_prose_with_gate_notice`), never substituted — the visible transcript may not contradict the applied `state_updates`. Follow-up *suggestions* on gate turns remain engine-owned replacements (the #430 ownership decision) — the prose composition must not be extended to suggestions. | §1.2 *Pending transition confirmation*; #656 (`case_5db5417fe445` turns 10-13) | **Code-guarded** — (a) the 0b handler in `_process_turn_impl` (shared `stripped_message`/`message_is_substantive` classification + `pending_transition["re_presented"]` cap) cancels and falls through instead of looping; `_matches_gate_token` (word-boundary) + the bare-confirmation check in `_user_confirms_transition` close the confirm side; (b) the override branches route prose through `_prose_with_gate_notice`, pinned by a source-level invariant (no `agent_response_text = metadata[…]` assignment anywhere in `_process_turn_impl`). *Composition seam (residual, Phase-1 scope):* the IntentResolver LLM-classifier tier can emit `confirmation=True` for typed text with no substance guard — the deterministic tiers and the pattern matchers carry the guarantee today; unify gate-answer classification there when Phase 1 lands. | `test_pending_gate_escape_lane.py` (13 tests incl. the two live incident messages, the confirm-side reproducers, and the matcher contracts); `test_gate_prose_composition.py` (composer contract + source-level invariant + the #430 suggestion-ownership boundary); `test_resolution_gate_close_escape.py::test_readiness_verdict_recorded_*` (transition_compliance readiness fields) |
| INV-27 | **A root that restates the case frame is never ADMITTED to a validated conclusion (§7.1 restatement guard — an entry bar).** A ROOT whose statement carries `< ROOT_NOVELTY_MIN_FRACTION` novel content tokens beyond the case frame (problem anchors + OTHER standing hypotheses; frame ownership excludes the root's attached hypothesis and any unattached mutual mirror of it) is held at INCONCLUSIVE by the empirical lane and never stamped by `validate_by_exclusion`. ENTRY-bar semantics: an already-validated root is ruled by evidence alone (no sibling-emission retraction; pre-guard persisted conclusions grandfathered), and the conclusion mirror follows the validation verdict (`synthesize_rcc_from_validated_root` re-adjudicates nothing; `retract_stale_engine_rcc` clears mirrors on evidence-driven demotion). KNOWN LIMITS: lexical only (synonym/filler-padding escapes; no-hypothesis disjunctions pass; dense sibling frames can delay validation) — semantic layers tracked on #656. Calibration figures live in `test_restatement_guard_calibration.py` (§7.1 prose). | `two-dimensional-hypothesis-methodology.md` §7.1 | **Code-guarded, one predicate, two entry points** — `causal_graph.root_restates_case_frame` / `_restating_root_ids` (per-derive precompute) read by `derive_node_states` (block-event counter `root_validation_blocked_restatement_total`) and `validate_by_exclusion`; `milestone_engine._recompute_cause_state_from_chain` runs `retract_stale_engine_rcc` every recompute. | `test_restatement_guard_calibration.py` (FP/TP/boundary/padding/dilution pins); `test_derive_node_states.py` (incident block, mechanism + owner-exclusion controls, sibling-emission monotonicity, counter semantics); `test_deductive_validation_wiring.py::test_exclusion_refuses_a_restating_survivor` (+ control); `test_chain_cause_state.py` (mirror-follows-validation + evidence-driven retract + LLM-RCC untouched) |
| INV-28 | **The engine never claims "verified" above the cause's assurance grade (M2 confidence discipline).** The engine-synthesized `RootCauseConclusion`'s confidence is DERIVED from the M2 grade, never asserted: a `MECHANISTIC` root (empirical or deductive validation, no counterfactual) mints `CONFIDENT` at a fixed 0.8 — a cap, so the LLM's own `root_cause_likelihood` cannot raise the mirror into "verified" — and only a `CONFIRMED` root (root-linked `causal_absence` SUPPORTS) mints `VERIFIED` (floor 0.9). A standing engine mirror whose confidence disagrees with the current grade is re-minted (upgrade on confirmation; correction of pre-cap persisted over-claims). The grade is persisted each recompute (`InvestigationProgress.cause_assurance`), so the grade × conclusion-confidence seam is queryable; a conclusion claiming `VERIFIED` while the grade is below `CONFIRMED` (only an LLM-authored conclusion can produce this now) is emitted at WARNING (`cause_confidence_overclaim`) on the TRANSITION into over-claim (edge-triggered via the persisted `cause_overclaim` flag — a standing over-claim warns once, not per turn) and flagged per turn in the grounding trace (`seam_overclaim`, same shared predicate), and the resolution report renders an assurance qualifier under Root Cause for any below-`CONFIRMED` conclusion (grade RECOMPUTED from the graph at render time — terminal blobs may predate the persisted field). Because the prompt records the resolution `causal_absence` row UNLINKED, the engine itself attaches the confirmation link — but ONLY at RESOLVED transition execution, on the user's explicit confirmation: `confirm_root_from_resolution_absence` (called from `_execute_resolved_transition`) links an unlinked absence row to the SOLE standing validated root and re-persists the grade (confirm-side twin of the M6 refute stamp). A row's mere appearance never confirms (an LLM self-claim; a premature 'stable now' row emitted mid-rollout — observed live in the gate sims — must not upgrade the grade); multiple validated roots → never guess, stays MECHANISTIC; REFUTES-linked rows never flip. | `two-dimensional-hypothesis-methodology.md` §0 M2, §9.5 | **Code-guarded** — `cause_assurance.grade_cause_assurance` / `root_counterfactually_confirmed` (single classifier, bearing-checked: the absence row must be LINKED to the root); `causal_graph.synthesize_rcc_from_validated_root` (grade-derived confidence + grade-agreement re-mint; expected level derived from the SAME mint constants via `ConfidenceLevel.from_score` so check and mint cannot drift; root selection prefers the CONFIRMED root, then the prior mirror's root — a level-only correction never swaps the named cause); `causal_graph.confirm_root_from_resolution_absence` (the confirm-side stamp); `milestone_engine._recompute_assessment_state` (persist + WARNING); `report_generation_service._assurance_note`. LLM-authored conclusion retraction is out of scope here (tracked on #656). | `test_chain_cause_state.py` (mechanistic cap incl. LLM-likelihood cap, confirmed→VERIFIED, mirror upgrade/downgrade, LLM-RCC untouched, grade persistence + blob round-trip, over-claim warning + control); `test_derive_node_states.py` (grade ladder + bearing); `test_resolution_assurance_note.py` (report qualifier) |
| INV-29 | **A single self-labeled causal datum never validates a ROOT (§7.1 independent-support bar).** Empirical ROOT validation requires `ROOT_INDEPENDENT_CAUSAL_SUPPORT_MIN` (= 2) INDEPENDENT causally-grounding supports: distinct `CAUSAL_EVIDENCE` rows, each linked SUPPORTS at `stance_confidence >= CAUSAL_STANCE_CONFIDENCE_MIN` (a self-hedged link is not grounding), whose contents are not mutual restatements of each other (`_EVIDENCE_MIRROR_JACCARD` over `summary + extract` tokens, counted as a maximum independent set of the pairwise mirror graph — order-invariant and MONOTONE under added evidence (a bridge row paraphrasing two independent observations never reduces the count); unjudgeably-short rows count ZERO). Non-ROOT rungs keep the >=1 bar; the deductive lane (§7.1.1) is untouched; a counterfactually CONFIRMED root (engine-stamped `causal_absence` SUPPORTS — M2 top grade, engine-only producer per INV-28) satisfies the bar outright, and at RESOLVED execution the confirm-stamp may target the SOLE count-held root (`support_count_held_root_ids` — blocked ONLY by this bar) since the user's gone⇒gone handshake is the decisive second observation; the count bar never vetoes the confirmation. Count-held roots are also exempt from anti-anchoring forced retirement and annotated with their recovery action in the context builder. RECOMPUTE-HONEST (no grandfathering, unlike INV-27): support counts are monotone in evidence, so a pre-bar 1-support VALIDATED root demotes to INCONCLUSIVE (live candidate, `cause_state=CANDIDATES` — never refuted; NO-COLLAPSE-safe) and `retract_stale_engine_rcc` clears its engine mirror. Companion flat-axis discipline (#573 B1): a direct LLM likelihood RAISE on a hypothesis with no confident grounding on EITHER belief axis (a SUPPORTS link at `stance_confidence >= CAUSAL_STANCE_CONFIDENCE_MIN` on the flat list OR a confident CAUSAL_EVIDENCE SUPPORTS on its chain root) is capped at `max(current likelihood, NEW_HYPOTHESIS_MAX_PRIOR)` — a ceiling on raises only, never a demotion below the earned value; updates apply AFTER the same turn's flat AND chain link passes, re-checking terminal immutability at apply time (a hypothesis auto-REFUTED by this turn's links is never resurrected) — the creation cap's "only evidence lifts belief past the gate" contract now holds on the update path too, with `system_feedback` telling the model to link evidence instead of re-asserting. | `two-dimensional-hypothesis-methodology.md` §7.1 (*Independent-support bar*); #573; #656 | **Code-guarded** — `causal_graph._node_evidence_tally` (confidence filter + per-evidence dedup), `_independent_causal_support_count` / `_causal_evidence_tokens` (mutual-mirror collapse), the ROOT branch in `derive_node_states` (block-event counter `root_validation_blocked_support_count_total`, labeled by reason `count`/`mirror_collapse`/`hedged_only` via the shared `_support_block_reason`/`root_support_block_reasons` classification the count-held set and the context annotation also read; fired only when the generic bar passed and real causal-category support exists); `terminal_transitions.finalize_resolution_truth_surface` (the ONE resolution finalizer shared by the chat executor and `ApiCaseService.close_case`: stamp + grade set + grade-keyed cause_state coherence incl. hypothesis-less confirmed roots + `cause_assurance._mint_confirmed_mirror` so a CONFIRMED terminal blob always carries a conclusion); `hypothesis_manager.update_hypothesis_likelihood` (evidence-free cap, counter `hypothesis_likelihood_capped_no_evidence_total`, progress judged on the APPLIED value so capped re-requests do not reset stagnation decay); `milestone_engine._apply_hypothesis_updates` (cap feedback to the LLM). | `test_derive_node_states.py` (1-support block + counter, 2-independent validate, mirror collapse, stance-confidence filter, rung >=1 control, confirmed-root bypass, deductive control); `test_chain_cause_state.py` (pre-bar root demotes on recompute + mirror retract); `test_hypothesis_prior_cap.py` (evidence-free cap, supported update uncapped, progress-on-applied); `test_hypothesis_update_apply_layer.py` (cap feedback surfaced + control, deferred-after-links ordering); `test_evidence_independence_calibration.py` (mirror/independence pair pins + knob boundaries) |
| INV-30 | **A causal-absence self-claim carries no unilateral force (§7.2 absence-trust discipline).** Three coupled bars, one per consumer of `causal_absence_evidence`: **(a) refute side** — an absence-backed REFUTES link is DECISIVE (single-handedly REFUTES the node, zeroes `belief` for §7.1.1 proof-by-exclusion, triggers the M6 node-side demotion, marks "refuted territory" for the §7.1 block-reason classifier) only at `stance_confidence >= CAUSAL_STANCE_CONFIDENCE_MIN` (`counterfactual_link_decisive` — the refute-side twin of INV-29's SUPPORTS filter); a self-hedged one stays ORDINARY refuting evidence (still feeds `refutes > supports` and `_net_refuted`), and the engine's own M6 links (no declared confidence → 1.0) stay decisive by construction. **(b) gate side** — the resolution-readiness READY bar and the closure→resolve pivot read the SHARED `resolution_confirmation_rows` qualification: non-engine-authored (`ENGINE_EVIDENCE_AUTHOR`), not itself a failed-fix disconfirmation (`_disconfirmation_row_ids` — REFUTES-linked on EITHER belief axis to the cause the ENGINE marked disconfirmed; a REFUTES link to a SIBLING is proof-by-exclusion, so the dual-use "fix worked, so it wasn't X" row stays confirmable — a blanket REFUTES-linked exclusion regressed READY right after the user confirmed), and at-or-after the latest ENGINE-KNOWN failed-fix disconfirmation (`latest_disconfirmation_turn` = newest engine-authored M6 row; authorship-keyed so node pruning cannot collapse the window and an LLM sibling-exclusion note recorded after a legitimate confirmation cannot retroactively mask it; `>=` so the mixed single-turn "first fix failed, second worked" shape is not stranded). The engine marker is reliable: `_attach_engine_refutation`'s idempotence is scoped to the ENGINE's own row, so M6 mints its marker even when the LLM already recorded the failure decisively (suppressing the mint left the window unset and re-qualified stale premature rows — reviewed regression). A failed fix the engine never saw (cause never grounded → M6 never fired) sets no window and marks no rows — accepted self-claim residual, stated in the predicate docstrings. Before this row, ANY absence row satisfied the gate — including the engine's own M6 DISCONFIRMATION rows, so a failed fix made the case read resolution-READY. **(c) stamp side** — at RESOLVED execution the confirm-stamp applies two root-scoped bars: the PER-ROOT REFUTATION WINDOW (`_root_disconfirmation_turn`, strict `>`): the cited row must be NEWER than any refutation recorded against the root being confirmed, any author, any confidence — a hedged self-claimed failed fix does not demote the root (§7.2) but the top grade is never minted from a row at-or-before it (this also excludes rows REFUTES-linked to the target chain itself; rows SUPPORTS-linked anywhere are never double-booked); and CONTENT BEARING (`_select_bearing_row`, a MIS-CITATION guard — the handshake is the trust bar): the citation is the NEWEST candidate not affirmatively about a DIFFERENT chain (≥2 shared content tokens with another chain's node/hypothesis statements while <2 with the frame = root statement + mechanism rungs + attached hypotheses + problem anchors → REFUSED); recency beats specificity, so an older frame-echoing premature row never outranks the user's actual latest confirmation, and a terse generic row is accepted (NO-COLLAPSE, the INV-29 count-held rescue); refusal never blocks the resolution, and the grade stays honest (MECHANISTIC with a standing validated root; NO_ROOT on the count-held shape — the held root stays INCONCLUSIVE, no conclusion minted). The report's "Confirming Evidence" section (both the LLM `evidence_basis` branch and the category fallback) renders an absence row only when it passes the gate-side qualification, and falls back to the category scan when the polarity filter empties the cited list (a disconfirmation must not render as confirmation; the section must not silently vanish). | `two-dimensional-hypothesis-methodology.md` §7.2/§7.3, §9.5; #656 | **Code-guarded** — `cause_assurance.counterfactual_link_decisive` (read by `causal_graph._node_evidence_tally` and `_node_has_counterfactual_refute`; the belief-zeroing and §7.1.1 exclusion follow the tally); `cause_assurance.resolution_confirmation_rows` / `has_resolution_confirmation` / `latest_disconfirmation_turn` / `_disconfirmation_row_ids` / `ENGINE_EVIDENCE_AUTHOR` (read by `terminal_transitions._has_causal_absence` → `assess_resolution_readiness` + `assess_closure_readiness`, and by the report's confirming-evidence filter); `cause_assurance._root_disconfirmation_turn` + `_select_bearing_row` (confirm-stamp row selection); `causal_graph._attach_engine_refutation` / `_node_has_engine_counterfactual_refute` (engine-marker mint, engine-scoped idempotence); `report_generation_service` polarity filter on both branches + filtered-empty fallback. *Outcome telemetry:* `counterfactual_refute_hedged_total` (ingest-time, one per created hedged link), `absence_confirmation_bearing_rejected_total` (one per refused row per stamp evaluation — retried resolves re-count) — see `lifecycle-metrics.md`. | `test_absence_bearing_calibration.py` (frame/generic/elsewhere corpus pins, veto-immunity floor boundaries, recency-over-specificity, scoped disconfirmation + dual-use-row pins, per-root window pins, same-turn + late-sibling-exclusion gate pins); `test_derive_node_states.py` (hedged-refute decisive-power pins); `test_chain_cause_state.py` (M6 hedged no-demote, engine-attach-over-hedged, engine-marker-over-LLM-decisive); `test_causal_graph_ingestion.py` (hedged-counter ingest pin); `test_resolution_causal_absence_gate.py` (gate qualification on the readiness surface); `test_resolution_assurance_note.py` (report polarity + filtered-empty fallback) |
| INV-31 | **More than one simultaneously-validated DISTINCT root never identifies a cause (§7.1.2 MECE arbitration).** Roots are mutually-exclusive origins (S2): at most one can be the real cause, so several standing at VALIDATED means the evidence has NOT discriminated — the forward mirror of the §7.1.1 exclusion collapse. While contested, EVERY case-level assertion of the cause is withheld: `cause_state` reads CANDIDATES (never IDENTIFIED, never flapped to UNKNOWN), the engine conclusion mirror is withheld from BOTH ends (`retract_stale_engine_rcc` clears a standing one naming a contested root; `synthesize_rcc_from_validated_root` refuses to mint one — asserting one of N equally-validated exclusive causes is an arbitrary pick, the DF-3 dict-order defect), and the ENGINE-GENERATED working conclusion stops counting as a known cause in `terminal_transitions._cause_identified` (it is the max-likelihood pick over the very hypotheses under contest — the same arbitrary assertion through a side door; the fallback rescues under-reporting, and a contest is a deliberate hold). LLM-authored conclusions are untouched (tracked on #656). The persisted `cause_identification_contested` flag records CONTEST EXISTENCE — the same predicate every behavioral consumer acts on, independent of the symptom anchor (behavior and observability keyed apart is how holds go invisible) — and the block-event counter edge-triggers on it. NODE states are untouched (each root's VALIDATED standing is ruled by its own evidence — the INV-27 entry-bar lesson: re-adjudicating settled nodes causes flap). The contest population is the VALIDATED roots of STANDING hypotheses (the same population that grounds `cause_state=IDENTIFIED` and the same standing preference the confirm-stamp applies). NOT a contest (`distinct_cause_clusters`, order-invariant connected components): duplicate emissions (mutual-mirror statements at `_ROOT_DISTINCT_JACCARD` — holding would deadlock, nothing can discriminate a statement from its own restatement; the bar is LEXICAL, so opposite-polarity statements read as one cause — accepted token-layer limit shared with INV-27/INV-29, pinned), a deepened chain (two ROOT-typed nodes on one LIVE causal path, either direction, via `_live_descendant_ids` — a path through a REFUTED rung does NOT connect, the link is disproven; deliberately stricter than the INV-30 bearing-frame walk which renders recorded mechanism), and a counterfactually CONFIRMED root anywhere (the gone⇒gone confirmation IS the discrimination — validated siblings never hold a proven cause hostage; on a reopened case a stale confirmation still settles, recurrence is discharged by the M6 failed-fix machinery); an unjudgeably-short statement merges with nothing (the safe failure is holding, never an arbitrary conclusion). Escape paths: decisive refutes on the alternatives, counterfactual confirmation of one root, or duplicate merge. RESOLUTION IS NEVER BLOCKED: the readiness gate keys on the confirmation row, and the confirm-stamp applies the SAME clustering — several candidate nodes collapse to distinct causes first; a sole cluster confirms at its ORIGIN (`sole_cluster_origin`/`_origin_of`: most live in-cluster descendants by the same reachability the cluster count used — one relations pass; the hook is read defensively, missing registration degrades to the safe refusal); idempotence and the refutation window are CLUSTER-wide (a confirmation or failed-fix refute anywhere in the cluster belongs to the CAUSE — a duplicate node never lets the same cause stamp twice, and a hedged refute on a duplicate windows the mint exactly as one on the cited origin would); an engine mirror naming ANOTHER member of the just-confirmed cluster is re-minted to the origin (same cause, wrong depth — the frozen-mechanistic-mirror shape); a duplicate/deepened node shape never vetoes the user's handshake (the INV-29 stamp-veto lesson) while a genuine multi-cause contest refuses (the case terminates MECHANISTIC, honestly). The stamp writes the durable confirmation link onto ONE member of a duplicate cluster — a future node-dedup/merge must migrate evidence_links to the surviving node (constraint noted at the stamp). The context builder renders the discrimination ask inline on contested roots through the same reason→note maps as the §7.1 recovery notes (elicitation: the model must see WHY identification is held). | `two-dimensional-hypothesis-methodology.md` §7.1.2, §9.2, §9.5; #656 | **Code-guarded** — `causal_graph.mece_contested_root_ids` / `distinct_cause_clusters` / `sole_cluster_origin` / `_origin_of` / `_live_descendant_ids` / `_edge_adjacency` (+ `_ROOT_DISTINCT_JACCARD`); the uncontested condition in `milestone_engine._recompute_cause_state_from_chain` (contest computed ONCE per recompute and threaded; edge-triggered WARNING `cause_identification_mece_hold` + counter `cause_identification_held_mece_total`, persisted flag `InvestigationProgress.cause_identification_contested`, `mece_contested` in the grounding trace); `causal_graph.retract_stale_engine_rcc` (contested-mirror retraction) + `synthesize_rcc_from_validated_root` (contested mint refusal); `terminal_transitions._cause_identified` (working-conclusion proxy gated on the persisted flag); the cluster-aware sole-cause check in `cause_assurance.confirm_root_from_resolution_absence` (via the `sole_cluster_origin` graph hook; cluster-wide idempotence + refutation window; cluster-member mirror re-mint); `context_builder._build_causal_graph_block` (contested-root annotation). | `test_mece_arbitration.py` (banked-specimen hold + node-states-untouched, edge-triggered counter/WARNING, contest-exists flag independent of symptom anchor, duplicate + deepened-line + refuted-rung + untokenizable + negation-limit + order-invariance cluster pins, refute/confirmation escape paths, mirror retraction + synthesize refusal + LLM-conclusion survival, working-conclusion proxy gate, progress-blob round-trip, cluster-aware stamp: duplicate confirm / origin citation / mixed-cluster origin / cluster-wide idempotence + window / mirror re-mint / distinct refusal, context annotation); `test_chain_cause_state.py` (distinct-cause stamp refusal, count-held sole-candidate discipline, mirror-selection under confirmation, contest-release-on-demote) |
| INV-32 | **`solution_proposed` reflects a LIVE fix offer, never a write-once latch (#656 DF-3).** The indicator is engine-DERIVED at the end-of-turn assessment recompute: True iff a SOLUTION-typed `ProposedAction` stands in liveness (`pending` = offer awaiting execution; `accepted` = executed fix, a historical fact never withdrawn) OR the gate ladder has advanced (`solution_accepted`/`solution_verified` are forward-only compliance facts — the INV-24 discipline — so the state_validator orderings `accepted ⇒ proposed` and `verified ⇒ proposed` hold by construction). `solution_state` mirrors it (`SELECTED`/`UNKNOWN`; `CANDIDATES` reserved). Two supersession events end an offer's liveness, each stamped on the action (`superseded_reason`/`superseded_in_turn`) and counted (`solution_offer_superseded_total{reason}`): **reproposal** — a new SOLUTION offer supersedes every prior pending one (the newest proposal IS the offer; same-turn multi-emission counts intra-turn siblings — documented in the metrics doc); **license_lost** — the M5 creation license (`_solution_cause_validated` → the shared `_cause_identified` predicate) is RE-CHECKED each recompute AFTER the cause recompute settles this turn's truth, and a pending offer whose established cause fell (M6 failed-fix demotion, conclusion retraction, MECE hold, restatement retraction) is WITHDRAWN rather than kept standing as "awaiting execution" for a cause the engine no longer asserts — the #656 frame latch dissolved at its root. TIMING EDGES (accepted, documented at the withdrawal): a license resting solely on the working-conclusion proxy clears on the FOLLOWING turn's recompute (the proxy regenerates after the recompute) — the Zone-3-pending frame still exits same-turn via `cause_state`. (A standing LLM-authored conclusion USED to keep the license through a MECE hold; **INV-34** closed that leak — `_cause_identified` read-suppresses a contested LLM conclusion, so the license falls and the offer is withdrawn like the working-conclusion leg.) The withdrawal notice is PREPENDED to `system_feedback` (turn-record truncation keeps the head), names NO mechanism (the license can fall to demotion, retraction, a MECE hold, or working-conclusion decay — the engine cannot always tell which), and the withdrawn offer leaves the `<pending_action>` block and the Zone-3-pending frame conjunction. ACCEPTED same-turn edge (documented): an offer emitted in the very turn that knocks its cause down is admitted on the prior turn's truth and withdrawn in the same recompute — the assistant's already-delivered prose may describe that fix for one turn; the next turn carries the notice and no pending block. ACCEPTED offers are never withdrawn by liveness rules: an executed fix's truth surface is the M6 counterfactual machinery. MITIGATION/DIAGNOSTIC actions never feed the indicator and are never superseded by these events. COMPANION HARDENING (the seams the flip-back made live): stage-gate compliance signals are applied AFTER the solutions step (`_apply_stage_gate_signals` — the guards must see actions created THIS turn: the prompt's KB-resolution flow mandates SolutionToAdd + `solution_accepted` in ONE response, and a pre-solutions snapshot deterministically rejected that bundle while telling the LLM to re-propose what it had just proposed), with IDEMPOTENCY FIRST (a re-emitted, already-registered signal is absorbed silently — rejecting it produced false "was not registered" feedback inviting a redundant re-proposal) and TYPE-MATCHED guards via the single `_GATE_ACTION_TYPE` table consumed by both the guards and the acceptance targeting — `solution_accepted` requires and accepts the most recent pending SOLUTION action, the mitigation-accept signal a pending MITIGATION (the old any-type guard/pick could stamp "accepted" on a never-executed SOLUTION — a permanent liveness source re-latching the dissolved frame — and let compliance register against a 3C/3D-downgraded DIAGNOSTIC, defeating the downgrade contract); rejected gate milestones surface `system_feedback` (a user reporting execution of a since-withdrawn offer must be told why nothing registered, not silently dropped), a rejected mitigation-accept SUPPRESSES same-turn `mitigation_verified` under the SAME notice (processing it would verify a stale record or fire the ordering guard's contradictory "set BOTH in the same response" advice), and a second workaround's accept against the standing single mitigation record (INV-24, no re-entry) is answered with feedback when a pending MITIGATION stands (the rendered MILESTONE_TO_SET affordance is otherwise a dead loop); `mitigation_verified` needs no pending action (its prerequisite is the accepted mitigation record — requiring one rejected every accept-then-verify-later flow); and `_maybe_propose_deferred_close` requires the same established-cause license (via the M5 wrapper `_solution_cause_validated` — creation gate, withdrawal re-check, and deferred-close gate call ONE function) (Solution records are monotone documentation and deliberately survive withdrawal, but a close message asserting "the root cause and fix are documented" must not fire on a cause the engine just retracted). Terminal coherence: the pair is derived by ONE shared function (`terminal_transitions.derive_solution_surface`) called from the per-turn recompute, the resolution finalizer, AND the CLOSED executor — the RESOLVED surfaces stamp the full ladder first (the user's confirmation is the compliance fact) so they derive SELECTED, while CLOSED freezes the derived truth without forcing the ladder (a standing pending offer reads SELECTED; a withdrawn one honestly does not — the documented fix lives on the monotone Solution rows); no recompute runs on a terminal case. NOT covered here: Zone-3-pending yielding on user contradiction/new evidence while the license still stands, and non-SOLUTION pending-action hygiene, are now **INV-33**; `solution_accepted`/TREATMENT re-entry after a failed fix is addressed by INV-33's TREATMENT-latch boundary (extended diagnosis lives in TREATMENT, §8.6 — no return to DIAGNOSIS by design). Since delivered by **INV-34** (#656 P2.3): LLM-conclusion refresh/retraction, incl. the M6 same-turn re-grounded-conclusion wipe (the demotion trigger now tracks the LLM's linked cause). Still routed: closure-readiness/report surfaces reading monotone `case.solutions` as documentation-of-record. | `investigation-lifecycle-logic.md` §1.4; `evidence-driven-investigation-framework.md` §4.2/§4.4; `two-dimensional-hypothesis-methodology.md` §7.3/§9.1; #656 | **Code-guarded** — `milestone_engine._live_solution_offer_exists` / `_supersede_pending_solution_offers` / `_withdraw_unlicensed_solution_offers` (M5 license re-check on the SAME `_cause_identified` predicate as the creation gate); the derivation in `_recompute_assessment_state` (runs after the cause recompute); the reproposal supersession at SOLUTION `ProposedAction` creation (3F write-once set removed); type-matched stage-gate guards + `_apply_stage_gate_side_effects` targeting; the established-cause gate in `_maybe_propose_deferred_close`; `ProposedAction.superseded_reason`/`superseded_in_turn` forensic stamps; counter `solution_offer_superseded_total{reason}` + WARNING `solution_offer_withdrawn` + `system_feedback` notices. | `test_solution_offer_liveness.py` (derivation through the real apply pipeline, reproposal + same-turn-multi-emission supersession, license-lost withdrawal + prepended feedback + labeled counter + WARNING event, same-turn create-then-withdraw, working-conclusion-proxy license survival + MECE-contested withdrawal, accepted-offer immunity, mitigation exclusion + survival, reproposal-spares-mitigation, `<pending_action>` block removal via the context builder, type-matched gate guards + rejection feedback + verify-without-pending, deferred-close license gate, SQLite round-trip of superseded stamps, zone-frame conjunction, helper-level liveness/supersession scope); `test_surgical_strip_and_cause_state.py::TestSolutionStateDerivation` (ladder-coherence, stale-flag flip-off); `test_solution_validation_gate.py` (M5 allowed-path license carried through the recompute) |
| INV-33 | **Zone-3-pending is a NON-suppressive hold, never a freeze (#656 DF-3 zone exit).** A pending SOLUTION proposal holds the investigation FOR THE FIX'S RESULT without foreclosing diagnosis. Two coupled properties. **(A) Prompt de-absolutization.** The DIAGNOSIS-stage focus emphasis (`_get_diagnosis_focus_emphasis`, `solution_proposed=True` branch) and the RCA "Propose Solution" standby clamp drop the absolutist "do not request further evidence / introduce alternative proposals" and the "offer exactly two suggestions — and no others (no new diagnostic asks)" gag that stranded the #656 diagnostic thread (turns 8–11: the resolv.conf finding emerged UNDER a pending fix and was suppressed). The replacement NAMES THE EXIT: if the user reports executing the fix, advance to TREATMENT; but if the reply instead brings new evidence, disputes the fix, or points at a competing cause, RESUME root-cause analysis on that signal — a pending proposal never gags a live diagnostic thread. The CHECK-BACK compliance rule and the FOLLOW-UP-AFTER-USER-ACTIONS block are softened in step: silence on a pending fix is still non-execution (hold), but a SUBSTANTIVE reply carrying new evidence/dispute/competing-cause is not silence — process it. The COMPLIANCE DETECTION rule is sharpened to match: the accept signal is EXECUTION of the fix (output PRODUCED BY running it — an executed-but-FAILED fix is still compliance, TREATMENT runs the failure analysis), and new DIAGNOSTIC evidence gathered WITHOUT executing the fix (a competing cause, a dispute, fresh data) explicitly REOPENS diagnosis rather than latching TREATMENT — without this carve-out the softened blocks and the unchanged "new evidence after the action → solution_accepted" rule collided on the word "new evidence" and a failed-then-new-cause reply could latch TREATMENT (the #656 failure mode). This is the SEMANTIC exit (the LLM judges "post-fix output vs. reopening evidence"); the STRUCTURAL exit — a cause that actually FALLS while a fix is pending — is INV-32's `_withdraw_unlicensed_solution_offers` (withdraws the offer, drops `solution_proposed`, exits the frame). The emphasis is DIAGNOSIS-only: once `solution_accepted` moves the case to TREATMENT, §8.6 owns the verify/extended-diagnosis prompt and the pending frame does not render (pinned) — a failed fix does extended diagnosis IN TREATMENT (no return to DIAGNOSIS by design), so the zone frame cannot bleed post-acceptance. **(B) Pending-action hygiene.** The `<pending_action>` render surfaces the newest `state=="pending"` action of ANY type; a standing SOLUTION offer shadows an earlier ask beneath it. When the offer LEAVES pending state the render falls through and RESURFACES the shadowed ask as the current compliance target — on WITHDRAWAL (license loss) it resurfaces in the re-opened DIAGNOSIS, and symmetrically on ACCEPTANCE the accepted offer (now `state=="accepted"`) stops covering the ask and it resurfaces in TREATMENT. `_retire_shadowed_diagnostic_asks` — called from BOTH the withdrawal path and the SOLUTION-acceptance side-effect — retires the shadowed **DIAGNOSTIC** asks (pending, `proposed_in_turn <` the offer's turn), stamped `superseded_reason="stale_pending"` and counted (`pending_action_superseded_stale_total`), so compliance reads a clean slate. STRICT `<`: a DIAGNOSTIC co-emitted in the offer's OWN turn is a live/reopening parallel thread (the de-absolutized prompt now invites it), not a shadowed pre-fix ask, so the same-turn create-then-withdraw edge preserves it. DIAGNOSTIC-ONLY by design: a DIAGNOSTIC ask has no compliance gate (never `accepted`), so retiring it is pure display cleanup with zero functional loss; a pending MITIGATION is cause-INDEPENDENT symptom relief the user may still execute, so it survives (INV-32) and its reappearance as the top pending ask is correct, not stale (the routed note's "or MITIGATION" was a symptom description — the principled resolution keeps the workaround live). Acceptance-retirement is SOLUTION-scoped, NOT mitigation: a SOLUTION accept moves the case to TREATMENT (diagnosis done, pre-fix asks stale), but a MITIGATION accept keeps it in active diagnosis where a shadowed DIAGNOSTIC is plausibly still live — retiring it there could drop a real ask; a genuinely-stale accumulation under a mitigation falls under the general no-lifecycle boundary below. The deeper root — DIAGNOSTIC asks carry no lifecycle and accumulate as `pending` for the life of the case, the newest-pending render only accidentally hiding them — is left standing; retirement on the two SOLUTION-transition boundaries closes the resurface class #656 exercises without inventing a general diagnostic-ask lifecycle. **Render selection** (`context_builder`): the `<pending_action>` block now prefers the newest COMPLIANCE-BEARING pending action (SOLUTION/MITIGATION, which carry a `MILESTONE_TO_SET`) over a bare DIAGNOSTIC — the de-absolutization lets the model raise a parallel diagnostic while a fix is still pending, and a plain newest-pending pick would let that diagnostic mask the fix's `solution_accepted` cue and stall the TREATMENT transition on the post-fix reply; a DIAGNOSTIC surfaces only when nothing compliance-bearing stands pending. | `evidence-driven-investigation-framework.md` §8.5/§8.6; `prompt-assembly-architecture.md` (zone table, KEY PRINCIPLES §8); `two-dimensional-hypothesis-methodology.md` §7.3; #656 | **Code-guarded** — `templates._get_diagnosis_focus_emphasis` (pending branch), `_RCA_DIAGNOSIS_BLOCK` "Propose Solution" step + CHECK-BACK + FOLLOW-UP blocks (de-absolutized); `milestone_engine._retire_shadowed_diagnostic_asks` called from `_withdraw_unlicensed_solution_offers` (pre-supersession newest-SOLUTION-turn cutoff) AND `_apply_stage_gate_side_effects` (the accepted SOLUTION's turn); the sharpened COMPLIANCE DETECTION block; `ProposedAction.superseded_reason="stale_pending"`; counter `pending_action_superseded_stale_total`; the compliance-bearing `<pending_action>` selection in `context_builder`; the DIAGNOSIS-only stage dispatch in `get_prompt_for_case` (TREATMENT → `TREATMENT_INSTRUCTIONS`, no pending emphasis). | `test_zone_exit.py` (emphasis drops the prohibition + names the exit; RCA clamp removed; accepted→TREATMENT stage + TREATMENT prompt omits the pending frame; stale DIAGNOSTIC retired on withdrawal AND on acceptance; same-turn diagnostic survives; diagnostic-after-offer survives; MITIGATION survives; counter per retired diagnostic; no-withdrawal leaves diagnostics untouched); `test_solution_offer_liveness.py` (mitigation-survives-license-loss boundary) |
| INV-34 | **An LLM-authored `RootCauseConclusion` is never authored by the engine, but is retracted on disconfirmation and read-suppressed while contested (#656 P2.3, §7.6 — retraction ≠ authorship).** A conclusion the LLM writes (`determined_by` = agent) is its own trust-boundary stance: the engine never writes or overwrites its `root_cause` text (`synthesize_rcc_from_validated_root` refuses it; the M2 grade labels confidence at read-time, INV-25). But *not authoring* ≠ *not retracting* — the conclusion the engine surfaces to every terminal consumer (disposition/M5 gate, report, copilot UI, KB harvest) still binds to the two guarantees. **(A) Disconfirmation → retract at source (NO-INCORRECT-CONCLUSION).** When the named cause is disconfirmed — M6 counterfactual refute / net-refutation (§7.3) — the conclusion is cleared for EVERY reader (one truth, no per-reader guard), exactly as an engine mirror is. **(B) Contest → read-suppress, not retract (NO-COLLAPSE).** A MECE hold (§7.1.2) is not disconfirmation, so the conclusion is PRESERVED (informational) but stops counting as identified in `_cause_identified` while contested, and re-counts the moment the contest resolves — erasing a reversible not-yet-arbitrated stance would collapse. **Mechanism — a conservative cause LINK.** An LLM conclusion arrives as free text with `validated_hypothesis_id` unset, so the link-based retraction (`retract_disconfirmed_rcc`) and the M6 `_representative_cause_hypothesis` pick cannot attribute it. Each recompute `link_llm_rcc_to_cause` (run FIRST, before M6) attributes the conclusion to the standing hypothesis it names — but ONLY on a SINGLE unambiguous match: exactly one standing hypothesis `>= RESTATEMENT_STRONG` with a substantive shared-token overlap (the orphan-chain T1 discipline). A wrong link would retract a valid conclusion on an unrelated refutation (a collapse), so *when unsure, don't link* — an unattributable free-text conclusion stays the documented residual it always was (no regression), and an already-live link is left stable. Linking is NOT authorship: the engine records only WHICH hypothesis the LLM's cause corresponds to, never the cause text. **Two consequences fall out of the link, not extra machinery:** the M6 blanket clear no longer wipes a conclusion RE-GROUNDED onto a different still-standing cause (the trigger now points at the LLM's actual named cause, not the max-`initial_likelihood` proxy that used to wipe it — the routed #656 residual); and because retraction is at source, every reader sees one truth. *Known limit (by design):* the link and the contest are LEXICAL, like every mirror bar — a synonym-paraphrased conclusion may not link and stays the residual; negation is stopworded. This closes the trust-boundary leak INV-32/INV-33 flagged (an LLM conclusion kept the M5 license through a MECE hold): with (B), `_cause_identified` is False while contested, so the M5 license (`_solution_cause_validated`) falls and INV-32 withdraws the pending offer for a contested LLM conclusion too. | `two-dimensional-hypothesis-methodology.md` §7.6/§7.1.2/§7.3/§9.3; #656 | **Code-guarded** — `causal_graph.link_llm_rcc_to_cause` (single-STRONG-match link, run first in `_recompute_cause_state_from_chain`); `retract_disconfirmed_rcc` (link-based, now reaches LLM conclusions + counts them); `demote_disconfirmed_cause_via_evidence` (M6 trigger tracks the linked cause; unconditional clear); `terminal_transitions._cause_identified` (contest gate hoisted BEFORE the RCC fallback = read-suppress); counters `llm_rcc_cause_linked_total` + `llm_rcc_retracted_disconfirmed_total`. | `test_llm_rcc_lifecycle.py` (single-STRONG link + ambiguous/weak/coincidental non-links + stable/dangling/engine cases; linked-conclusion retraction on refutation + counter; unlinked residual survives; recompute keeps a re-grounded conclusion on another standing cause; M6 clears same-cause + unlinked; contest read-suppress + reversibility); `test_mece_arbitration.py::test_neither_proxy_counts_while_contested` |
| INV-35 | **Cause identification is engine-derived, never LLM-declared (#656 P2.4, §7.7).** `cause_state=IDENTIFIED` is derived by the engine from a validated, uncontested chain root (§9.2 — two independent causal observations per INV-29, symptom verified, no equally-validated rival per §7.1.2), *never* from an LLM self-claim. There is **no LLM-settable "root cause identified" signal** — the former `MilestoneUpdates.root_cause_identified` boolean is removed from the structured-output schema (the engine already skipped it; it was dead). This is a guardrail, not a seizure of the LLM's judgement: the LLM decides *what* the cause is (authors hypotheses, builds/grounds the chain, states confidence in `root_cause_likelihood`, names the cause); the engine only certifies whether that decision cleared the grounding bar that unlocks the irreversible actions gated on identification (solution/terminal/harvest). Completes the campaign's relocation of guardrail *operation* off LLM self-claims (DF-1/DF-2) at the prompt/schema layer — the same move as INV-23/29/30/34, now for the identification signal. **The conclusion names its cause:** the LLM sets `names_root_node_id` on its `RootCauseConclusion` to the `cn_` root node it already emits during chain construction, and the engine attributes the conclusion to the hypothesis whose `root_node_id` matches — the authoritative link of §7.6, exact and guess-free, with the lexical scan (`link_llm_rcc_to_cause`) demoted to the fallback for id-less conclusions. Closes the DF-2 residual that the conclusion was authored in a namespace disconnected from the causal graph. *Known limit (by design):* the free-text conclusion persists as a terminal-soundness backstop while models under-build chains (`cause_state` is a SOFT signal); deriving the conclusion text from the chain (retiring dual-authoring) is the gated eventual convergence (§7.7 rejected-for-now). | `two-dimensional-hypothesis-methodology.md` §7.7/§9.2; `prompt-assembly-architecture.md`; #656 | **Schema + Code-guarded + Prompt** — schema: `MilestoneUpdates.root_cause_identified` removed, `RootCauseConclusionUpdate.names_root_node_id` added; code: `link_llm_rcc_to_cause` tier 1 sets `validated_hypothesis_id` from `names_root_node_id` via a standing-hypothesis `root_node_id` match guarded by substantive overlap (tier 2 lexical fallback for id-less conclusions), `_recompute_cause_state_from_chain` derives `cause_state` (never reads a self-claim), KB-remediation prefetch fires on the `cause_state`→IDENTIFIED edge; prompt: DIAGNOSIS teaches build-and-ground (no boolean), TREATMENT teaches `names_root_node_id`. Counters `llm_rcc_cause_named_total` (authoritative) + `llm_rcc_cause_linked_total` (lexical fallback). *Composition seam:* the prompt no longer references the decommissioned self-certification field — pinned by a contract test so the two layers cannot silently re-diverge. | `test_prompt_engine_realignment.py` (no `root_cause_identified` in schema/prompt; `names_root_node_id` authoritative link sets `validated_hypothesis_id`; id-less conclusion falls to lexical fallback; KB prefetch fires on cause_state edge); `test_llm_rcc_lifecycle.py` (authoritative link precedence over scan) |
| INV-36 | **One cause, one hypothesis: `hypotheses_to_add` is deduped against standing statements (#656, §7.8).** The work gate (§5.2) counts `len(case.hypotheses)`; a duplicated cause would spuriously satisfy the ≥2 gate that separates `INSUFFICIENT_EVIDENCE` from `NOT_YET_PRODUCTIVE` (observed live: an identical DNS hypothesis minted on turns 10/11 of the incident). At apply time the engine refuses to mint a hypothesis whose statement **duplicates** a standing one (same-batch duplicates fall out for free — a sibling minted earlier this turn is already in `case.hypotheses`). Because a dedup DROPS an LLM emission (unlike §7.1.2's reversible fold) the bar (`hypothesis_statements_duplicate`) is **stricter and fails open**: a *mutual mirror* at `_HYPOTHESIS_DUPLICATE_JACCARD` (0.8, well above the MECE fold's 0.6), so a genuinely-distinct short statement differing by one substantive token ("memory leak in connection pool" vs "… cache pool", Jaccard 0.6) survives; symmetric (not `restatement_score` containment) so a more-**specific elaboration** survives; a **polarity guard** refuses the dedup on asymmetric negation ("not" is stopworded); a **numeric-discriminator guard** keeps "server 1" vs "server 2" distinct (the tokenizer drops single digits); and **only standing causes** are targets (`REFUTED`/`RETIRED` are skipped — terminal-immutable, the update path says "open a NEW hypothesis", so deduping would deadlock a revival). The dedup collapses only genuine restatements, never diagnostic work (NO-COLLAPSE). A skip surfaces the matched id to the LLM (`system_feedback`: update it, don't restate), is **not** counted as generation (`hypotheses_generated` stays truly-new so a dedup cannot masquerade as progress and mask the exhaustion detector), and preserves positional integrity via a separate `hyp_emit_order` list that records the canonical existing id at the skipped item's slot (so a same-turn `new_index_N` ref resolves to the kept hypothesis, not the wrong sibling). A chain the LLM emitted for the duplicate is left to `resolve_orphan_chains` (which re-attaches only FLAT hypotheses under its `_hypothesis_lacks_real_chain` guard); the dedup does NOT re-root the canonical (that would bypass the guard and could GC a validated chain). | `two-dimensional-hypothesis-methodology.md` §7.8/§5.2; #656 | **Code-guarded** — `causal_graph.hypothesis_statements_duplicate` (0.8 mutual mirror + polarity + numeric guards) / `find_duplicate_hypothesis` (standing-only); `milestone_engine._apply_investigation_updates` `hypotheses_to_add` loop (skip + `system_feedback` + `hyp_emit_order`); the three hypothesis `new_index_N` resolution sites (updates, evidence links, need motivators) read `hyp_emit_order`. Counter `hypothesis_dedup_skipped_total`. | `test_hypothesis_dedup.py` (predicate calibration: exact/reorder dup, elaboration + short-sibling + negation + numeric-discriminator + empty survive; standing match, terminal ignored; apply-path: standing dup not minted + counter + feedback + not-counted-as-generation; intra-batch minted once; elaboration survives; cross-category no gate inflation; positional integrity `new_index_1` past skipped dup; refuted-cause revival mints; dedup does not re-root canonical) |

| INV-37 | **Closed-gate resolve-preservation: a resolvable case never terminally closes (#656).** The resolution gate is protective because RESOLVED asserts a positive conclusion ("it's fixed"); the closure gate is deliberately thin because CLOSED asserts nothing — so the closure path has no readiness/needs-info hold (closing is always a valid disposition and is never withheld to demand data). The one place the two gates must stay coupled is the **SUGGEST_RESOLVE pivot** (`assess_closure_readiness` → the case has a qualifying `causal_absence`, so it is resolution-grade): "resolve = close *with* resolution; close = close *without* resolution", and users have no incentive to close a case that could be resolved, so it is always safe to resolve a resolvable case whichever terminal was requested. That pivot fires at **proposal** time (LLM-emit + dropdown paths); INV-37 extends it to **confirm** time — a qualifying `causal_absence` can land *after* a close was proposed, so `confirm_pending_transition` re-checks at the single execution chokepoint immediately before the close commits. On a hit it does **not** close: it replaces the pending close with a RESOLVED proposal and reports that nothing terminal executed (returns `False`), and the caller re-presents the resolve confirmation. Scoped to `INVESTIGATING` (RESOLVED is not a valid edge from INQUIRY, and an INQUIRY case cannot carry a qualifying `causal_absence`). There is **no symmetric counterpart** — every case is closable, so "never close a resolvable case" has no "never resolve a closable case" mirror; the asymmetry is principled (a NO-INCORRECT-CONCLUSION guard on the disposition record, not a symmetry gap). | `investigation-lifecycle-logic.md` §1.2 (SUGGEST_RESOLVE pivot); #656 | **Code-guarded** — `terminal_transitions.confirm_pending_transition` (closed + INVESTIGATING + `assess_closure_readiness`==SUGGEST_RESOLVE → `propose_transition(resolved)` + return `False`); `milestone_engine` confirm callers (early gate confirm returns the resolve-confirmation reply; `_check_automatic_transitions` fallback sets `close_pivoted_to_resolve` + `override_suggestions` and does not mark `status_transitioned`); response builder appends the resolve confirmation via `_prose_with_gate_notice`. Counter `close_pivoted_to_resolve_total`. | `test_closed_gate_symmetry.py` (chokepoint pivot returns False + pending→resolved + counter; genuine close still closes; pending-resolve unaffected; INQUIRY-scope never proposes invalid edge; engine wiring surfaces the resolve DECIDE pair without marking transitioned) |
| INV-38 | **The NOT_YET_PRODUCTIVE vacuum drives a pull-back, never a silent spin (#656 P3.1, DF-6 gap A).** `assess_verification_status` returns `NOT_YET_PRODUCTIVE` whenever the §5.2 work gate hasn't passed — but that status fired from turn 1 and *drove nothing*, so a model that never forms a hypothesis (`case_5db5417fe445`: 0 hypotheses across 13 INVESTIGATING turns) evaded every stuck-case net (the insufficient-evidence handoff, exhaustion, and deadlock each require ≥2 hypotheses) and spun silently. The corrective: once the vacuum has PERSISTED past the stall thresholds (`is_stalled` — the same `EXHAUSTION_MIN_TURNS`/`EXHAUSTION_STALL_THRESHOLD` floor the insufficient-evidence handoff uses, so the status becomes actionable only after it has genuinely stuck, not on every early turn), the engine substitutes a code-guarded pull-back affordance pair pointed at symptom / expected-vs-observed clarification — re-establishing the basis a first hypothesis needs. Scoped to the true **0-hypothesis** vacuum (`not case.hypotheses`): a case with ≥1 hypothesis has a diagnostic direction and needs breadth/discrimination, not a symptom re-elicitation — pulling it back would be wrong, so it is out of scope by design. **Accepted residual (stated, not silently introduced):** the below-work-gate-but-*nonempty* band — 1 hypothesis, or ≥2 in a single category, or ≥2 with too little evidence — also reads `NOT_YET_PRODUCTIVE` yet receives no corrective from any net (the insufficient-evidence handoff / `EXHAUSTED` / deadlock all require the ≥2 work gate). The pull-back is deliberately NOT extended to it (its corrective would be *discrimination*, not symptom re-elicitation — a distinct, lower-stakes concern), so that band's silent-spin is a smaller, separately-trackable gap, not one INV-38 closes. INVESTIGATING-scoped and ordered LAST beside the insufficient-evidence handoff; the two are **mutually exclusive by construction** (0 hypotheses vs the ≥2 work gate). Keep-engaging moves only — never steering toward close (D4 soft-collapse): the vacuum is the engine's failure to elicit, not the case's to abandon. This closes the §5.5 carve-out ("an empty-graph model lands in NOT_YET_PRODUCTIVE, never INSUFFICIENT_EVIDENCE — it cannot exercise the intended fire"): the empty-graph corner now has its own net. | `insufficient-evidence-handling.md` §5.2/§5.6; #656 | **Code-guarded** — `milestone_engine._hypothesis_vacuum_pending` (INVESTIGATING + 0 hypotheses + `is_stalled` + status==NOT_YET_PRODUCTIVE) / `_hypothesis_vacuum_suggestions` (FREE_SPEECH pull-back pair); the `engine_owned_affordances` `"not_yet_productive"` branch (below `insufficient_evidence`) + the driver recording `verification_status` on fire. Counter `engine_owned_affordance_served_total{gate="not_yet_productive"}` (the existing per-gate affordance metric; no new counter). | `test_engine_owned_affordances.py::TestNotYetProductivePullback` (fixture reads NOT_YET_PRODUCTIVE; pull-back fires with gate label + pair; no fire before the stall floor; no fire with a standing hypothesis; INVESTIGATING-scoped; insufficient-evidence + disposition precedence; keep-engaging non-close FREE_SPEECH) |

**Drift notes (as of this writing):**

- **INV-01** historical drift: the design previously described a mechanical regex fallback (`user_confirms()`) inside `_apply_inquiry_updates` as the second confirmation path. That fallback was deliberately removed in commit `06cfa834` (2026-03-17) in favor of intent-routing for explicit clicks. The pseudocode at the top of §1.2 still references the old fallback and should be updated separately. The same-turn-confirmation guard documented in INV-01 was added in `13ff2eae` after the gap was observed in production.
- **INV-01** deferral-without-recovery gap *(resolved)*: when commit `13ff2eae` added the same-turn-confirmation guard, its commit message stated *"the transition is deferred to the next turn (where the LLM re-presents the statement and the user confirms explicitly)."* That deferral assumption was never code-backed. The recovery turn's context still carried the `NOT_YET_CONFIRMED` block from `16bb0912` (2026-04-23), which actively instructs the LLM *"Do NOT re-propose the same statement."* When the guard fired, the case stalled silently — statement persisted, no LLM re-present, no clickable affordance. Observed on case `case_bb917dcd5bb2` (turn 14 "Yes, let's investigate this"): guard fired, user moved on to other questions, case never transitioned. Fix: `case.inquiry.handshake_deferred_at_turn` flag set at the guard site; `context_builder` switches to `HANDSHAKE_DEFERRED` on the recovery turn; engine deterministically emits the canonical confirmation suggestions. The matrix row above now lists both Code-guarded layers (deferral + recovery affordance) so future audits surface the prompt-code dependency before a similar regression slips through.
- **INV-04** matrix-text drift *(resolved)*: the INV-04 row now names the real symbols (`ALLOWED_ACTIONS` + `is_valid_action()`), and §1.3's pseudocode block now uses `ALLOWED_ACTIONS` instead of the non-existent `VALID_TRANSITIONS`. The duplication-risk note below is the remaining open item — graph still lives in three places, gated by `test_inv04_valid_action_graphs_agree_across_definitions`.
- **INV-04** duplication risk: the valid-action graph is duplicated across **three** locations — `ALLOWED_ACTIONS` (case_action_manager.py), `valid_actions` (inside `is_valid_action()` in models.py), and implicit in the `_execute_*_transition` runtime preconditions in `terminal_transitions.py`. They currently agree, but no single source of truth means a future single-sided edit would let the forbidden edge slip through one enforcement surface while the others still reject it. `test_inv04_valid_action_graphs_agree_across_definitions` is the consistency guard until consolidation; the cleanup itself is separate work.
- **INV-04** dead code *(resolved)*: `CaseActionManager.validate_action` (and its `validate_transition` alias) had zero production callers and was deleted. `ALLOWED_ACTIONS` is now used only by the UI adapter for `get_allowed_transitions` (dropdown affordance), which was always its only real purpose.
- **INV-04** doc-doc contradiction *(resolved)*: `agent-stage-playbook.md` previously declared an `INQUIRY → TERMINAL (RESOLVED)` "Fast-track: KB match" edge in three places (§1 transition table, phase diagram, and INQUIRY-stage Gate Conditions table). The edge does not exist — INV-04's Pydantic validator + `_execute_resolved_transition`'s precondition reject it at construction time. The playbook was describing the **UX appearance** ("inquiry resolved fast") while the code implements INV-06's same-turn KB-Resolution collapse (INQUIRY → DIAGNOSIS → RESOLVED, one turn, but still passing through INVESTIGATING). Doc-behind-code drift; playbook updated to remove the fast-path with explanatory call-outs pointing readers at the canonical spec here. Surfaced by a separate design-check that streamed the finding but did not save it to the audit report — also a reminder that the saved report is not always a complete record of the streamed audit.
- **INV-05** (stage gates) intentionally relies on prompt-only enforcement. The blast radius is bounded — stage transitions within INVESTIGATING don't change disposition or commit anything irreversible. A premature gate at most miscategorizes the current investigation phase; it cannot leak a case into a terminal state. Documented here so the asymmetry with INV-03 is deliberate, not accidental.
- **INV-06** *(resolved)*: the prior drift between §1.2's claim of a same-turn user-side collapse and the engine's `transition_proposed_this_turn` guard has been closed. The engine now fires `confirm_pending_transition` in the same turn ONLY when both `transition_proposed_this_turn` and `knowledge_resolution_signalled` are set in the turn's metadata — the well-scoped KB-resolution path. Every other ProposedTransition emission still follows the standard 2-turn handshake. As a side effect, `metadata["knowledge_resolution_signalled"]` (previously dead) is now load-bearing as the conjunction gate. Decision history: Option B from the cluster-2 audit follow-up; recency of design (2026-05-11) over code (2026-02-12) + recoverability of the code change (no edge-case dead end) drove the choice to update code rather than weaken the design.
- **INV-07** schema permissiveness *(decided — keep current)*: `InquiryStateUpdate` uses Pydantic's default `extra='ignore'`. An LLM emitting `evidence_to_add` (or any other invalid field) in an INQUIRY response has the field silently dropped, not rejected. The invariant holds (no Evidence row created — the engine has no creation branch either). Switching to `extra='forbid'` was considered and rejected: the invariant is enforced by **field absence**, not by rejecting strays; switching to `forbid` would convert benign LLM drift into user-facing turn failures (the `_generate_structured_output` retry chain might recover, but each occurrence costs latency + cost or surfaces as an error) with no improvement to the invariant. If LLM drift becomes a concern, add a separate passive observability signal (e.g., log unknown fields at the structured-output parse step) — not a Pydantic config tightening.
- **INV-09** terminology drift *(resolved)*: the INV-09 row now describes the mechanism accurately as "`require_case_not_terminal()` helper function (used at the case-update endpoint; other write endpoints inline the `case.is_terminal` check)". A future consolidation pass via a FastAPI dependency-injection layer remains deferred — the protection is consistent in intent across endpoints today, only the mechanism varies.
- **INV-14** *(resolved)*: previously two drift notes lived here — (a) §1.5.2 still described the deprecated plain-text `/queries` mechanism, and (b) §1.5.2 didn't surface the RESOLVED dropdown's three-branch behavior (`READY` / `SUGGEST_CLOSE` / `NEEDS_INFO`). Both gaps are now closed: §1.5.2 Step 2 describes the structured-intent payload (`intent_type="status_transition"` + `intent_data`) and Step 3 carries the three-branch table.
- **INV-15** (advisor role) is acceptable as prompt-only because the worst-case symptom is the agent suggesting it ran a command (it didn't — there's no execution surface in the codebase to do so). The constraint is stylistic; violations are caught in compliance review, not at runtime.
- **INV-15** scan scope *(decided — keep narrow, defer broader signal)*: the runtime `_completion_phrases` scan (defined at `milestone_engine.py:3088`, consumed at `:3148`) covers the high-stakes transition-completion phrases (*"case closed"*, *"marking as resolved"*) where the signal/noise ratio is strongest. The broader `_ADVISOR_ROLE_CONSTRAINT` banned-phrase list (*"Let me check"*, *"I will run"*, *"Let me look at"*, *"I'll execute"*) is deliberately NOT in the runtime scan — these phrases are generic enough to appear in benign context (the agent quoting the user, hedge phrasing like *"Let me look at this from your angle..."*) where false positives would dilute the telemetry signal. If broader advisor-role drift detection becomes valuable, add a separately-tagged `advisor_role_compliance` signal alongside the existing `transition_compliance` scan — distinct log key so analytics can pivot on each independently. Deferred until operational evidence warrants it.
- **INV-17 / INV-19 / INV-20 / INV-21 retired (2026-06-05, flow redesign):** these rows enforced the path fork and its emission bans — INV-17 (hypothesis-before-causal-evidence, enforced across the path-conditional dispatch), INV-19 (Gate 2 path-selection commit), INV-20 (already removed), and INV-21 (Gate 3 post-mitigation RCA guard). The unified opportunistic flow removes `InvestigationPath` / `PathSelection`, Gate 2, Gate 3, and the three path-conditional emission bans entirely. **R6 tier shift (deliberate):** retiring INV-17 and INV-21 drops two *structural/code* guards. They are replaced by (a) the `cause_state` gate — run the diagnostic machinery iff `cause_state ∈ {UNKNOWN, CANDIDATES}` (a prompt-guided rule, no longer a hard engine ban on hypothesis/causal-evidence emission), (b) the per-milestone surgical strip (INV-23), and (c) the coupled prompt mandate that forces hypothesis emission under uncertainty (the producer of derived `CANDIDATES`). The self-naming simulation campaign is the safety net for this tier change. The hypothesis-before-causal-evidence ordering survives as prompt guidance in the unified INVESTIGATION block, not as an engine reject-and-resurface backstop.

**How to use this matrix:**

1. When a PR touches any code referenced under "Enforcement", verify the corresponding row's classification still holds — a row that moves to a weaker category is a deliberate design change requiring a doc update.
2. When writing tests, prefer pinning invariants over pinning behaviors. A test named after an INV-XX is robust against incidental refactors; a test named after the function under test is not.
3. When the design evolves, add or remove rows. The matrix is not a frozen artifact — but each change should be visible in a single doc diff.
4. When introducing a new cross-tier dependency (one tier's enforcement cooperates with another tier's behavior to achieve the design goal), add a `*Composition seam:*` annotation to the row's Enforcement column. The annotation names the dependency, states what removing/weakening one side would do (distinguishing invariant violation from design-goal degradation), and points to the symbols future audits should verify together. See INV-01, INV-03, INV-06, INV-14 for examples. Absence of the annotation on a row means the invariant was reviewed and found to be single-tier or to have intra-tier concerns captured in the drift notes.
