# Core FastAPI and server dependencies
fastapi>=0.115.8
uvicorn>=0.28.0
python-multipart>=0.0.9

# --- NEW: Agent Framework ---
langgraph>=0.1.2 # Critical for the stateful agent architecture

# --- NEW: LLM Providers ---
fireworks-ai>=0.12.1 # For the primary Firework AI provider
openai>=1.0.0 # For OpenAI API compatibility
anthropic>=0.25.0 # For Anthropic Claude API
tiktoken>=0.5.0 # For accurate OpenAI token counting

# --- NEW: Security (PII Redaction) ---
# Note: PII protection now uses K8s Presidio microservice instead of local libraries
# Local Presidio dependencies removed - using HTTP API to presidio.faultmaven.local

# --- NEW: Data Processing & Analysis ---
numpy>=1.26.4, <2
pandas>=1.5.0
pandas-stubs>=2.0.0
pylogrus>=0.1.2 # For log parsing
scikit-learn>=1.5.0 # For anomaly detection
pyod>=2.0.1 # For anomaly detection
protobuf>=3.20.0,<3.21.0

# --- NEW: Knowledge Base & Embeddings ---
chromadb>=0.5.3 # Vector store
sentence-transformers>=3.0.1 # For creating embeddings
pypdf>=4.2.0 # For processing PDF documents
python-docx>=1.1.2 # For processing DOCX documents

# --- NEW: Observability ---
opik>=0.2.1 # For Comet Opik tracing
prometheus-client>=0.20.0 # For exposing Prometheus metrics

# --- NEW: Enhanced Logging ---
structlog>=24.1.0 # For structured logging
python-json-logger>=2.0.7 # For JSON log formatting

# --- NEW: Session Management ---
redis[hiredis]>=5.0.0 # For distributed session storage

# --- NEW: Retry Logic ---
tenacity>=8.3.0 # For robust retry mechanisms

# HTTP requests and utilities
requests>=2.31.0
httpx>=0.28.1
aiohttp>=3.9.3
anyio>=4.3.0
pydantic<2.10,>=2.6
typing-extensions>=4.12.2
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
beautifulsoup4>=4.12.3
tavily-python>=0.7.0

# LangChain and related integrations
langchain>=0.1.15
langchain-community>=0.0.32
langchain-openai>=0.1.3
langchain-huggingface>=0.0.3
langchain-anthropic>=0.1.5
